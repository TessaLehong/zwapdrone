const drone = document.querySelector('.drone');

// No need for flash-related functions anymore

// Trigger any other actions if needed when the drone animation ends
drone.addEventListener('animationiteration', () => {
    // You can add other actions here if needed
});
